-- Scenario 2:

-- Create record
INSERT INTO TRANSACTIONS VALUES(451, CURRENT_DATE, 4500, 'Sav', 500);

-- Update values
UPDATE ACCOUNTS
SET BALANCE = BALANCE - 500
WHERE ACCOUNT_NUMBER = 4500;

-- Create record
INSERT INTO TRANSACTIONS VALUES(452, CURRENT_DATE, 4500, 'Chq', 500);

-- Update values
UPDATE ACCOUNTS
SET BALANCE = BALANCE + 500
WHERE ACCOUNT_NUMBER = 1145;

commit;
