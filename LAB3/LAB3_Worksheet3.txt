INSERT INTO CUSTOMER 
VALUES('829', 'Ibruli', 'Noel', '132 Castlefield', 'Toronto', 'ON', 'M4R1G7', 1000, 88888, '58'); --Foreign Key and Check Constraint will not work

SELECT * FROM CUSTOMER;

SELECT * FROM SALESREP;

INSERT INTO ORDERS
VALUES(87654, CURRENT_DATE, '730'); --Foreign Key constraint will not work

SELECT * FROM ORDERS;

--Correct Statements

DELETE FROM CUSTOMER 
WHERE CUSTOMER_NUMBER = '829';

DELETE FROM ORDERS
    WHERE ORDER_NUMBER = 87654;

ALTER TABLE CUSTOMER 
    ADD CONSTRAINT CUSTOMER_CREDIT_LIMIT_CK
     CHECK (CREDIT_LIMIT < 77777);
     
ALTER TABLE CUSTOMER
    MODIFY CUSTOMER_NUMBER VARCHAR(3); --Data types now match


ALTER TABLE ORDERS 
	ADD CONSTRAINT ORDERSFK1 
	FOREIGN KEY( CUSTOMER_NUMBER ) 
	REFERENCES CUSTOMER ( CUSTOMER_NUMBER ); --adding foreign key constraint
    
ALTER TABLE CUSTOMER
    MODIFY SALES_REP_NUMBER CHAR(3); --Data types now match
    
ALTER TABLE CUSTOMER
    ADD CONSTRAINT CUSTOMERFK
    FOREIGN KEY (SALES_REP_NUMBER)
    REFERENCES SALESREP (SALES_REP_NUMBER); --adding foreign key constraint
    
ALTER TABLE SALESREP
    ADD PHONE_NUMBER NUMBER(10, 0) DEFAULT '4164915050' NOT NULL;  --adding a phone number column with the default being the company number
    
CREATE TABLE SALESREP2 AS (SELECT * FROM SALESREP); --creating backup
    